<Window x:Class="StockView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StockView"
        xmlns:uc="clr-namespace:StockView.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <TabControl >
            <!--<TabItem DataContext="{Binding Children[0]}" x:Name="ConfigureCompanyList" Header="StockViewerManagement" >
                <uc:EditCompanyListView/>
            </TabItem>-->
            <TabItem DataContext="{Binding Children[0]}" x:Name="StockDataList" Header="StockList" >
                <uc:StockDataView/>
            </TabItem>
        </TabControl>

    </Grid>
</Window>
